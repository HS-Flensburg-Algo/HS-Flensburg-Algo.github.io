\documentclass[tikz,convert={outfile=\jobname.svg}]{standalone}

\usepackage{minted}
\usepackage{tikz}

\begin{document}
\small
\begin{tikzpicture}[level/.style={sibling distance=20cm/(2^#1), level distance=1.5cm}, font=\scriptsize]
\node {\texttt{subsetsum(7, 0)}}
    child{ node {\texttt{subsetsum(5, 1)}}
        child{ node {\texttt{subsetsum(4, 2)}}
            child{ node {\texttt{subsetsum(-1, 3)}}
                child{ node {\texttt{false}}
                }
            }
            child{ node {\texttt{subsetsum(4, 3)}}
                child{ node {\dots}}
                child{ node {\dots}}
            }
        }
        child{ node {\texttt{subsetsum(5, 2)}}
            child{ node {\texttt{subsetsum(0, 3)}}
                child{ node {\texttt{true}}
                }
            }
            child{ node {\texttt{subsetsum(5, 3)}}
                child{ node {\dots}}
                child{ node {\dots}}
            }
        }
    }
    child{ node {\texttt{subsetsum(7, 1)}}
        child{ node {\dots}
        }
    };
\end{tikzpicture}

\end{document}