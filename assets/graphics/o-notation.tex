\documentclass[tikz,convert={outfile=\jobname.svg}]{standalone}

\usepackage{minted}
\usepackage{tikz}
\usetikzlibrary{intersections}

\begin{document}
\begin{tikzpicture}
    \draw[->]  (0,0)coordinate(O) -- (8,0);
    \draw[->]  (0,0) -- (0,7);

\draw[domain=0:8,smooth,variable=\x,blue,name path=c1] plot ({\x},{0.5*\x+2*sin(\x r)+1})node[right,black]{$c \cdot g(x)$};
\draw[domain=0:8,smooth,variable=\x,red,name path=c2] plot ({\x},{0.2*\x+0.5*sin(\x r)+2})node[right,black]{$f(x)$};
\fill[red,name intersections={of=c1 and c2}]
    % (intersection-1) circle (2pt)
    % (intersection-2) circle (2pt)
    (intersection-3) circle (2pt);

\draw[dashed] (intersection-3) -- (intersection-3|-O) node[below]{$n_0$};
\end{tikzpicture}
\end{document}