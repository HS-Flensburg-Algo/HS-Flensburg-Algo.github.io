\documentclass[tikz,convert={outfile=\jobname.svg}]{standalone}

\usepackage{minted}
\usepackage{tikz}

\begin{document}
\small
\begin{tikzpicture}[->,>=stealth,level/.style={sibling distance=8cm/#1, level distance=1.5cm}]
\node {\texttt{fib(5)}}
    child{ node {\texttt{fib(4)}}
            child{ node {\texttt{fib(3)}}
            	    child{ node {\texttt{fib(2)}}
                            child{ node {\texttt{fib(1)}} }
                            child{ node {\texttt{fib(0)}} }
                    }
	    	    child{ node {\texttt{fib(1)}} }
            }
            child{ node {\texttt{fib(2)}}
		    child{ node {\texttt{fib(1)}} }
		    child{ node {\texttt{fib(0)}} }
            }
    }
    child{ node {\texttt{fib(3)}}
            child{ node {\texttt{fib(2)}}
		    child{ node {\texttt{fib(1)}}}
		    child{ node {\texttt{fib(0)}}}
            }
            child{ node {\texttt{fib(1)}} }
     };
\end{tikzpicture}
\end{document}